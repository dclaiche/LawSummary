To run:

  1. Copy backend/.env.example to backend/.env and fill in your API keys
  2. Run scripts/dev.sh to start both servers, or run them separately:
    - Backend: cd backend && source .venv/bin/activate && uvicorn main:app --reload --port 8000
    - Frontend: cd frontend && npm run dev